# Reveriebot runs with ZERO dependencies. Nothing here is required.
# These are optional upgrades - the app detects each at runtime and uses it
# if present, otherwise falls back to a stdlib path.

# pypdf - cleaner PDF text extraction for open-access papers.
# Without it, a built-in stdlib extractor reads born-digital PDFs but loses
# ligatures (fi/fl) and some structure. With it, extraction is clean and
# captures the full text. The reader shows which backend ran ("pdf · pypdf"
# vs "pdf · stdlib").
pypdf>=4.0
